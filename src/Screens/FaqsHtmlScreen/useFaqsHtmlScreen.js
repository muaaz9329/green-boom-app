import React, {useState} from 'react';
import {Text, View} from 'react-native';
import {hp, wp} from '../../Config/responsive';

export const useFaqsHtmlScreen = ({navigate, goBack}, {params}) => {
  // const [faqData, setFaqData] = useState();
  // setFaqData(params);

  return;
};
